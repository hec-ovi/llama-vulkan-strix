name: llama-vulkan-strix

# Compatibility entry point. The stock Vulkan stack is now the default in
# docker-compose.yml, so plain `docker compose up -d` starts the same service.

services:
  vulkan-llm:
    extends:
      file: docker-compose.yml
      service: llm

networks:
  llmnet:
    driver: bridge

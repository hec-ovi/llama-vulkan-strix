# local config (copied from .env.example)
.env
.env.bak
# local compose merge (never commit a machine-specific override)
docker-compose.override.yml

# research store (may hold private findings); see .research/INDEX.md
.research/

# python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/

# model weights and run artifacts never live in git
# Leading slash: only the repo-root models/ mount, not compose/models/
/models/
*.gguf

# os
.DS_Store
